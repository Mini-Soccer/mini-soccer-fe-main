### perintah build dockerfile
```bash
docker build --no-cache -t mini-soccer-project/fe:latest .
```

### perintah menjalankan container
```bash
docker compose up -d
```

### konfirmasi semua containers didalam 1 services ini berjalan
```bash
docker compose ps
```
